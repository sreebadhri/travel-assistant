# Core dependencies — pinned to exact versions for reproducibility.
# Update deliberately: review changelogs, test, then bump versions.
# Run `pip-audit` periodically to check for known vulnerabilities.

langchain==1.2.13
langchain-openai==1.1.11
langgraph==1.1.3
python-dotenv==1.2.2

# Testing
pytest==9.0.2
pytest-mock==3.15.1
